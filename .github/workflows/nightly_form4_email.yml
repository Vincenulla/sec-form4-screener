name: "SEC Form 4 Screener – Email + GitHub Pages"

on:
  schedule:
    - cron: "0 20 * * *"   # 22h heure de Paris (20h UTC)
  workflow_dispatch:       # Permet de lancer manuellement

jobs:
  run-screener:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 reportlab

      - name: Run SEC Form 4 screener
        run: |
          mkdir -p reports
          python sec_form4_screener_reportlab_summary.py

      - name: Read summary
        id: summary
        run: |
          echo "text<<EOF" >> $GITHUB_OUTPUT
          cat summary.txt || echo "Aucun résumé généré." >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Send PDF report by email
  uses: dawidd6/action-send-mail@v3
  with:
    server_address: smtp.gmail.com
    server_port: 465
    username: ${{ secrets.EMAIL_USERNAME }}
    password: ${{ secrets.EMAIL_PASSWORD }}
    subject: "📈 Rapport quotidien Form 4 – Achats > 100k$"
    to: ${{ secrets.EMAIL_TO }}
    from: "SEC Screener <${{ secrets.EMAIL_USERNAME }}>"
    body: ${{ steps.generate.outputs.email_body }}
    attachments: Form4_Report.pdf, email_summary.txt

      - name: Generate HTML page
        run: |
          python publish_html.py

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: public
