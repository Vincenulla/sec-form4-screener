name: SEC Form 4 Screener – Email + GitHub Pages

on:
  schedule:
    - cron: "0 22 * * *"  # tous les jours à 22h UTC
  workflow_dispatch:

jobs:
  nightly:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Cloner le dépôt
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Vérifier le répertoire courant
      - name: Show current directory
        run: pwd

      - name: List repo files
        run: ls -l

      # 3️⃣ Installer Python et dépendances
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests reportlab

      # 4️⃣ Exécuter le script Python depuis la racine
      - name: Run SEC Form 4 Screener
        run: python ./sec_form4_screener_reportlab_summary.py

      # 5️⃣ Envoyer l'e-mail avec PDF + résumé
      - name: Send PDF report by email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "📈 Rapport quotidien Form 4 – Achats > 100k$"
          to: ${{ secrets.EMAIL_TO }}
          from: "SEC Screener <${{ secrets.EMAIL_USERNAME }}>"
          body: |
            Voici le résumé des achats insiders du jour (>100k$).
            Le rapport PDF détaillé est joint.
            ---
            $(cat email_summary.txt)
          attachments: Form4_Report.pdf, email_summary.txt
